$(document).ready(function () {

    var table = $('#dashboardTblId').DataTable({
        //"createdRow": function (row, data, dataIndex) {
        //    if (data[9] == "Scheduled") {
        //        $(row). .addClass("scheduledColour");
        //    }
        //}
    });
});